<% title "Lists" %>

<table>
  <tr>
    <th>Pubblicata</th>
  </tr>
  <% for list in @lists %>
    <tr>
      <td><%= list.pubblicata %></td>
      <td><%= link_to "Show", list %></td>
      <td><%= link_to "Edit", edit_list_path(list) %></td>
      <td><%= link_to "Destroy", list, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New List", new_list_path %></p>
