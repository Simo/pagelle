<% title "Pagella del #{@pagella.pubblicata}" %>

	<% @voti.each do |f| %>
		<div id="voto_<%=f.id%>">
			<%= link_to 'VOTO', edit_vote_path(f.id, :format => :js), :remote => true %>
			<%= f.level_id %> - <%= f.note %>: <%= f.motivo %>
		</div>
	<%end%>
	<div id="voto"> c </div>